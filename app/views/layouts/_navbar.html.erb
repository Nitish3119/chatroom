<% if logged_in? %>
  <div class="ui small nav-col menu">
    <div class="ui container">
      <b><%= link_to "Pending Requests(#{current_user.pending_request.count})", all_requests_user_path, class: "item" %></b>
      <div class="right menu">
        <div class="menu nav-col">
          <%= link_to "Logout", logout_path, class: "item", data:{ turbo_method: :delete} %>
        </div>
      </div>
    </div>
  </div>
<% end %>